<div id="navbar">
  <div class="row">
    <div class="col-md-3">
      <nav class="navbarNav">
          <%= link_to 'Home', root_path, class: "navbarAnchor" %> 
          <%= link_to 'Users', users_path, class: "navbarAnchor" %>
          <%= link_to 'Posts', posts_path, class: "navbarAnchor" %>
      </nav>
    </div>

    <div class="col-md-6">
      <h1>Coffee Meets Human</h1>
    </div>

    <div class="col-md-3">
      <nav class="navbarNav">
          <%= link_to 'Signup', signup_path, class: "navbarAnchor" %>
          <% if logged_in? %>
            <%= link_to "Log out", logout_path, method: :delete, class: "navbarAnchor" %>
          <% else %>
            <%= link_to "Log in", login_path, class: "navbarAnchor" %>
          <% end %>
      </nav>
    </div>

  </div>
</div>